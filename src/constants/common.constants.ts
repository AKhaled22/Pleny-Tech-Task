// Array fields that should use $in operator for filtering
export const arrayFields: string[] = [
  'cuisines',
  'favoriteCuisines',
  'followers',
  'following',
];

// Add other field type definitions if needed in the future
